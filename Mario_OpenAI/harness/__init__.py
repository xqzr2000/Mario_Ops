"""
Mario_OpenAI gaming harness.

A perception -> memory -> reasoning harness around the same OpenAI vision
agent openai_play.py runs, after the architecture of lmgame-org/GamingAgent,
plus a guard and cross-run lessons. Entry point: ../harness_play.py.
See ../HARNESS.md.

Imports resolve against the Mario_OpenAI/ directory (config, openai_agent,
openai_play, video_utils), so run from there, as with openai_play.py.
"""
